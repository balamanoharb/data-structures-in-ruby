module DataStore
  module Heap
    # An array visualized as a nearly complete binary trees
    # Insert(S,x) x = element
    # Max(S)
    # ExtractMax(S)
    # IncreaseKey(s, x, k) k = for x to be reached
  end
end